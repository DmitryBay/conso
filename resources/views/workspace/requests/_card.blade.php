<article class="kanban-card" draggable="true" data-request-card data-status-url="{{ route('workspace.requests.status',$item) }}">
    <div class="d-flex align-items-start justify-content-between gap-2"><span class="priority-label priority-{{ $item->priority->value }}"><span></span>{{ __('workspace.priority.'.$item->priority->value) }}</span><span class="text-secondary" style="font-size:10px">#{{ str($item->public_id)->substr(0,6)->upper() }}</span></div>
    <a class="kanban-title" href="{{ route('workspace.requests.show',$item) }}">{{ $item->title }}</a>
    @if($item->description)<p class="kanban-description">{{ str($item->description)->limit(90) }}</p>@endif
    <div class="d-flex align-items-center gap-2 flex-wrap mt-3"><span class="room-chip"><i class="bi bi-door-closed me-1"></i>{{ $item->room_number }}</span>@if($item->due_at)<span class="due-chip {{ $item->isOverdue() ? 'overdue' : '' }}"><i class="bi bi-clock me-1"></i>{{ $item->due_at->format('H:i') }}</span>@endif</div>
    <div class="kanban-card-footer"><div class="d-flex align-items-center gap-2">@if($item->assignee)<span class="avatar avatar-sm">{{ mb_strtoupper(mb_substr($item->assignee->name,0,2)) }}</span><span class="text-truncate" style="font-size:11px">{{ $item->assignee->name }}</span>@else<span class="text-secondary" style="font-size:11px"><i class="bi bi-person-plus me-1"></i>{{ __('workspace.unassigned') }}</span>@endif</div>@if($item->status===\App\Enums\RequestStatus::New)<form method="POST" action="{{ route('workspace.requests.take',$item) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-primary py-1 px-2">{{ __('workspace.take') }}</button></form>@else<div class="dropdown"><button class="btn btn-light btn-sm py-1 px-2" data-bs-toggle="dropdown"><i class="bi bi-arrow-right"></i></button><div class="dropdown-menu dropdown-menu-end">@foreach(\App\Enums\RequestStatus::kanban() as $next)@continue($item->guest_stay_id && $next===\App\Enums\RequestStatus::Completed)<form method="POST" action="{{ route('workspace.requests.status',$item) }}">@csrf @method('PATCH')<input type="hidden" name="status" value="{{ $next->value }}"><button class="dropdown-item" @disabled($next===$item->status)>{{ __('workspace.status.'.$next->value) }}</button></form>@endforeach</div></div>@endif</div>
</article>
